package Ex03_Literals;

public class Lab32_Literals_P3 {
    public static void main(String[] args) {
        int Enum = 9; // enum is keyword but Capital Enum is not so we can use it
        System.out.println(Enum);
    }
}